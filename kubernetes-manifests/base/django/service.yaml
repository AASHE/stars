apiVersion: v1
kind: Service
metadata:
  name: stars
  annotations:
    cloud.google.com/neg: '{"ingress": false}'
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    app: stars
