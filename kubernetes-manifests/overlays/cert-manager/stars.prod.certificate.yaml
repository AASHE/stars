apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: stars-cert
  namespace: stars-prod
spec:
  secretName: stars-tls-secret
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging
  commonName: reports.aashe.org
  dnsNames:
  - reports.aashe.org