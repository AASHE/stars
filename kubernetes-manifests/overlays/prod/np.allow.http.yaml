kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  namespace: stars-prod
  name: allow-http-ingress
spec:
  podSelector:
    matchLabels:
      tier: django
  ingress:
  - from:
    - ipBlock:
        cidr: 0.0.0.0/0
    ports:    
    - protocol: TCP
      port: 8080